<% if @kitten.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@kitten.errors.count, "error") %> prohibited this kitten from being saved:</h2>
 
    <ul>
    <% @kitten.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: @kitten, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br><br>

  <%= f.label :age %>
  <%= f.number_field :age, min: 0, max: 30 %><br><br>

  <%= f.label :cuteness %>
  <%= f.number_field :cuteness, min: 0, max: 10 %><br><br>

  <%= f.label :softness %>
  <%= f.number_field :softness, min: 0, max: 10 %><br><br>

  <%= f.submit %>
<% end %>